import { Center } from '@mantine/core';

export function Logo() {
  return (
    <Center w={40} h={40} c="black" fw={700} fz={20} bg="stockragGreen.5" style={{ borderRadius: 8 }}>
      SR
    </Center>
  );
}
